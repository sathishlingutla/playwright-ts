function addition(a:any,b:any){
    return a+b;// number ct- type inference will be applied as number
}

addition(10,29);

function adds(a:number,b:number){
    return a+b;
}
function additions(a: number, b: number): number {
    return a + b;
}

addition(10, 9);

function add(a: number, b: number): number {
   console.log(a+b);
    return a + b
    console.log("Welcome back")
}